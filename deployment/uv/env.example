# Wind Market Wallboard backend (.env) template
# Copy this file to projects/wind-wallboard/backend/.env and adjust as needed.

# Data edition: select which data provider stack to load at runtime.
#   wind -> use WindPy provider (requires Wind terminal + credentials)
#   open -> use open/free API provider implementation
DATA_MODE=wind

# Redis caching controls. Set REDIS_ENABLED=true when a Redis instance is available.
# REDIS_URL follows redis-py URI format: redis://host:port/db
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379/0
# TTL (seconds) for cached snapshot payloads; ignored when Redis disabled.
SNAPSHOT_CACHE_TTL=15

# Wind credentials (only needed when DATA_MODE=wind).
# Provide your Wind terminal account and password if the provider expects login.
WIND_USER=
WIND_PASSWORD=

# API keys for open-data providers (optional but recommended for higher rate limits).
# Configure only the keys you intend to use in the Open edition implementation.
ALPHAVANTAGE_API_KEY=
FRED_API_KEY=
COINGECKO_API_KEY=
CME_FEDWATCH_API_KEY=

# Optional network proxy for markets with restrictive outbound access (leave blank if unused).
HTTP_PROXY=
HTTPS_PROXY=
